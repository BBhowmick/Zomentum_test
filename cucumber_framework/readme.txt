Prerequisites:

RVM installation
  \curl -sSL https://get.rvm.io | bash -s stable --ruby

set-ruby: If not already set (run rvm list to check which ruby is set as default)
	rvm use 2.6.3

Chrome should be updated

Selenium installation
  brew install selenium-server-standalone

go to cucumber_framework folder and rub command : make set-gems

got to project folder and rub command cucumber

